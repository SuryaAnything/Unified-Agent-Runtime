{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Proprio Capability Manifest",
  "description": "The response structure for the __proprio_manifest__ call.",
  "type": "object",
  "required": ["app_name", "version", "tools"],
  "properties": {
    "app_name": {
      "type": "string",
      "description": "The display name of the application."
    },
    "version": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+$"
    },
    "tools": {
      "type": "array",
      "description": "List of callable functions exposed to the AI.",
      "items": {
        "type": "object",
        "required": ["name", "description", "parameters"],
        "properties": {
          "name": { "type": "string" },
          "description": { "type": "string" },
          "parameters": {
            "type": "object",
            "description": "JSON Schema of the arguments this tool accepts."
          }
        }
      }
    }
  }
}